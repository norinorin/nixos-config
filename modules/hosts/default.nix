{
  den.hosts.x86_64-linux.toaster = {
    isMobile = true;

    # TODO: declare hardware like monitors, gpu paths for wms, etc
    # monitors = {};

    users.nori = {
      cursor = {
        pname = "bibata-cursors";
        name = "Bibata-Modern-Ice";
        size = 16;
      };
    };
  };
}
